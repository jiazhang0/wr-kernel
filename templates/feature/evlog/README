
                                   evlog
                                  ------

1. Introduction
----------------

The evlog template provides a mechanism to enable Event logging
in kernel on the target.


2. Usage
--------

Add this template using the following configure script option.
ie.: --with-template=feature/evlog

This will ensure the required kernel config values are set.


3. Installation
----------------

This template will include event logging in kernel image.

